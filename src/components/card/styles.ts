import Box from "@mui/material/Box";
import styled from "@mui/system/styled";

export const HeroCardContainerStyled = styled(Box)(() => ({
  display: "flex",
  flexDirection: "column",
  justifyContent: "center",
  alignItems: "center",
  backgroundColor: "#f5f5f5",
  borderRadius: "10px",
  padding: "20px",
}));
